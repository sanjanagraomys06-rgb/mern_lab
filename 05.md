# Question 5
```
Implement all CRUD operations on a File System using Node JS
```

# Install Package

```bash id="3rkydv"
npm install readline-sync
```

---

# File System CRUD Operations Using Node.js

This program:

* stores student records in `students.json`
* uses synchronous keyboard input
* implements:

  * Create
  * Read
  * Update
  * Delete
* menu driven program

---

# app.js

```javascript id="6l0mq1"
const fs = require('fs');
const readlineSync = require('readline-sync');

const FILE_NAME = 'students.json';

// Create file if not exists
if (!fs.existsSync(FILE_NAME)) {
    fs.writeFileSync(FILE_NAME, '[]');
}

// Read students from file
function readStudents() {
    const data = fs.readFileSync(FILE_NAME, 'utf8');
    return JSON.parse(data);
}

// Write students to file
function writeStudents(students) {
    fs.writeFileSync(FILE_NAME, JSON.stringify(students, null, 2));
}

// CREATE
function createStudent() {

    const students = readStudents();

    const usn = readlineSync.question("Enter USN: ");
    const name = readlineSync.question("Enter Name: ");
    const sem = readlineSync.questionInt("Enter Semester: ");
    const year = readlineSync.questionInt("Enter Year of Admission: ");

    const student = {
        usn,
        name,
        sem,
        year_of_admission: year
    };

    students.push(student);

    writeStudents(students);

    console.log("\nStudent Added Successfully\n");
}

// READ
function readAllStudents() {

    const students = readStudents();

    console.log("\nStudent Records:\n");

    if (students.length === 0) {
        console.log("No Records Found");
        return;
    }

    console.table(students);
}

// UPDATE
function updateStudent() {

    const students = readStudents();

    const usn = readlineSync.question(
        "Enter USN to Update: "
    );

    const index = students.findIndex(
        s => s.usn === usn
    );

    if (index === -1) {
        console.log("Student Not Found");
        return;
    }

    students[index].name =
        readlineSync.question("Enter New Name: ");

    students[index].sem =
        readlineSync.questionInt("Enter New Semester: ");

    students[index].year_of_admission =
        readlineSync.questionInt(
            "Enter New Year of Admission: "
        );

    writeStudents(students);

    console.log("\nStudent Updated Successfully\n");
}

// DELETE
function deleteStudent() {

    let students = readStudents();

    const usn = readlineSync.question(
        "Enter USN to Delete: "
    );

    const newStudents = students.filter(
        s => s.usn !== usn
    );

    if (students.length === newStudents.length) {
        console.log("Student Not Found");
        return;
    }

    writeStudents(newStudents);

    console.log("\nStudent Deleted Successfully\n");
}

// MENU
while (true) {

    console.log("\n===== STUDENT CRUD MENU =====");
    console.log("1. Create Student");
    console.log("2. Read All Students");
    console.log("3. Update Student");
    console.log("4. Delete Student");
    console.log("5. Exit");

    const choice = readlineSync.questionInt(
        "\nEnter Your Choice: "
    );

    switch (choice) {

        case 1:
            createStudent();
            break;

        case 2:
            readAllStudents();
            break;

        case 3:
            updateStudent();
            break;

        case 4:
            deleteStudent();
            break;

        case 5:
            console.log("Exiting Program...");
            process.exit();

        default:
            console.log("Invalid Choice");
    }
}
```

---

# Run Program

```bash id="t0kzyy"
node app.js
```

---

# Example students.json

```json id="u6zoy6"
[
  {
    "usn": "4SF22CS001",
    "name": "Somu",
    "sem": 5,
    "year_of_admission": 2022
  },
  {
    "usn": "4SF22CS002",
    "name": "Arun",
    "sem": 3,
    "year_of_admission": 2023
  }
]
```

---

# Features Used

* `fs.readFileSync()`
* `fs.writeFileSync()`
* `JSON.parse()`
* `JSON.stringify()`
* `readline-sync`
* Menu driven loop
* Persistent file storage

# Sample Input / Output

## Run Program

```bash id="gr6l2x"
node app.js
```

---

# Initial Menu

```text id="0ru4v2"
===== STUDENT CRUD MENU =====
1. Create Student
2. Read All Students
3. Update Student
4. Delete Student
5. Exit

Enter Your Choice:
```

---

# 1. CREATE Operation

## Input

```text id="5u4chm"
Enter Your Choice: 1

Enter USN: 4SF22CS001
Enter Name: Somu
Enter Semester: 5
Enter Year of Admission: 2022
```

## Output

```text id="bmxq6y"
Student Added Successfully
```

---

# students.json After Insert

```json id="kndv8d"
[
  {
    "usn": "4SF22CS001",
    "name": "Somu",
    "sem": 5,
    "year_of_admission": 2022
  }
]
```

---

# 2. READ Operation

## Input

```text id="pb2g72"
Enter Your Choice: 2
```

## Output

```text id="lmtrcx"
Student Records:

┌─────────┬──────────────┬────────┬─────┬───────────────────┐
│ (index) │ usn          │ name   │ sem │ year_of_admission │
├─────────┼──────────────┼────────┼─────┼───────────────────┤
│ 0       │ 4SF22CS001   │ Somu   │ 5   │ 2022              │
└─────────┴──────────────┴────────┴─────┴───────────────────┘
```

---

# 3. UPDATE Operation

## Input

```text id="vww22w"
Enter Your Choice: 3

Enter USN to Update: 4SF22CS001

Enter New Name: Arun
Enter New Semester: 6
Enter New Year of Admission: 2021
```

## Output

```text id="t8ml5v"
Student Updated Successfully
```

---

# students.json After Update

```json id="a0qbnd"
[
  {
    "usn": "4SF22CS001",
    "name": "Arun",
    "sem": 6,
    "year_of_admission": 2021
  }
]
```

---

# 4. DELETE Operation

## Input

```text id="2g63bg"
Enter Your Choice: 4

Enter USN to Delete: 4SF22CS001
```

## Output

```text id="yz1jlwm"
Student Deleted Successfully
```

---

# students.json After Delete

```json id="1jjlwm"
[]
```

---

# 5. EXIT

## Input

```text id="5xxhtl"
Enter Your Choice: 5
```

## Output

```text id="9ogsw5"
Exiting Program...
```
